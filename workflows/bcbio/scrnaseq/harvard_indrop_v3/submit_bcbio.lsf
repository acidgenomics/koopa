#!/bin/sh

#BSUB -q medium
#BSUB -J indrop
#BSUB -N
#BSUB -n 1
#BSUB -W 100:0
#BSUB -R "rusage[mem=10000]"
#BSUB -e project.err
#BSUB -o project.out

# Use 6x cores per sample
# 12 x 6 = 72

bcbio_nextgen.py ../config/indrop_rnaseq.yaml -n 72 -t ipython -s lsf -q medium '-rW=90:00' -r mincores=2 -r minconcores=2 --retries 3 --timeout 1000
