"""Install star-conda."""

from __future__ import annotations


def main(
    *,
    name: str,
    version: str,
    prefix: str,
    passthrough_args: list[str] | None = None,
) -> None:
    """Install star-conda (stub)."""
