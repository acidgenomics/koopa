# koopa

@AGENTS.md

## Claude-specific

- Plans/TODOs → `todo.org` (Org mode) at the repo root, NOT `.claude/todo.md`.
- After a correction, route the lesson to its home — skill, path-scoped rule, or slim
  core — per `.claude/rules/lessons.md`. Don't accumulate everything in one file.

Interaction style, verification principles, elegance, and bug-fixing guidance live in
the user's global `~/.claude` rules and are not restated here.
