#!/usr/bin/env bash
set -Eeu -o pipefail

# shellcheck source=/dev/null
source "$(_koopa_header harvard-o2)"

# Launch SLURM interaction session.
# Updated 2019-06-23.

# 1 core; 16 GB RAM; 6 hours.

srun -p interactive \
    --pty -c 1 \
    --mem 16G \
    --time 0-06:00 \
    "$SHELL"
