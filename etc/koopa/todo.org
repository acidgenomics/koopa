* High complexity
** DONE python has dual path installation using uv or building from source.
** DONE Think about configuration and trigger scope support for:
    - koopa configure user
    - koopa update user
* Medium complexity
** TODO Let's not trigger automatic configuration our emacs and vim apps during install.
** TODO Confirm install support for emacs and vim interfaces:
    - doom-emacs
    - spacemacs
    - prelude-emacs
    - spacevim
** DONE Deferred: doom_emacs, python_app (need new abstractions)
    Homebrew Emacs and MacVim GUI support for doom-emacs, spacemacs, spacevim.
** DONE `koopa` entrypoint needs to be smarter about loading venv to make tqdm accessible when running outside of development repo where we have `.venv` loaded with direnv. Need to double check this.
* Low complexity
** TODO vim maps to _conda, not vim.py. That means vim.py is a dead file.
    Review if we have any other dead installer files like this that are safe to remove.
** DONE Need to fix broken app links in website documentation.
** DONE See if we need to clean up some man1 links in app.json
    pigz (broken man1 symlink: pigz.1)
    rclone (broken man1 symlink: rclone.1)
    tree (broken man1 symlink: tree.1)
** TODO koopa update isn't catching this issue on macOS
    ~/.local/share/koopa/share/man/man1 develop f047d7e [!] <python 3.12.13: .venv>
    $ koopa system check
    fontconfig is not linked at /Users/stmich14/.local/share/koopa/opt/fontconfig
    Update will install 1 apps: fontconfig.
    Warning: System checks completed with warnings.
    ** Run 'koopa update' to resolve these issues.
** TODO Consider installing emacs from conda-forge instead of building from source.
    We need to evaluate if GUI usage is useful, otherwise switch to conda build, which only supports terminal.
