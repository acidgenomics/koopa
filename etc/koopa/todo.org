#+TITLE: koopa
#+STARTUP: content
* High priority
** TODO We need to make sure that koopa develop push-app-build is smart and knows to reupload a build after a revision bump.
** TODO We need to look for situation where current user has ~/.config/koopa/activate symlinked to KOOPA_PREFIX/activate. We need to update this to resolve to KOOPA_PREFIX/activate.sh during our `koopa update` call. Otherwise current user shell configuration will get messed up. Dotfiles will then get correctly refreshed at end of `koopa update` call. This is an edge case where we don't want to break koopa activation for previous users during our new update here.
** TODO Did our recent conda channel handling updates totally mess up conda cache handling? I don't want conda installs to cache anything, they blow up disk space. Or maybe it makes sense to let the installers cache and then blow the conda cache out in a single call in `koopa update` instead.
* Medium priority
** TODO Do we need to configure custom SSL_CERT_FILE support in our bootstrap environment? Review again that our current implementation is correct. Mark done when all tasks on builder machine finish successfully.
** TODO Enhance `koopa update` defaults to only run certain extra features like dotfiles config when `--extra` flag is set.
** TODO Let's review supported flags for `koopa update`. We shouldn't use `--extra` that's too vague. Let's switch this to `--dotfiles`. Also, our S3 cleanup step should be manual, it's kind of slow.
** TODO Consider not putting any other koopa command line apps in `bin` except koopa. Things like `rename-snake-case`.
** TODO Review our options for supporting multiple koopa installs on a single machine. Current problem is that user config goes to ~/.config/koopa and really only supports one active koopa instance.
* Medium priority
** TODO Remove legacy support for `koopa header` returning bash, python, r code. Ideally when we do this, we want to deprecate, and show the user a useful error message. Previous use of "koopa header bash" may not have 'set -Eeuo pipefail' above, and we don't want this to run through for legacy scripts.
** TODO Need to fix jekyll deploy code in koopa-acidgenomics-com repo.
* Low priority
** TODO Improve our code coverage with additional unit tests.
