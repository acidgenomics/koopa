* High complexity
* Medium complexity
** DONE Rework bootstrap.sh to use uv and bundled astral Python when not in a corporate environment.
** DONE Check gcc install support on macOS.
** DONE We need a way to retrigger reinstall of all default apps and any other optional apps currently installed by the user on the system. Basically a total app rebuild trigger that automatically resolves depdency logic and rebuilds everything in order.
** DONE We need to come up with a new idea for 'koopa install-all-apps'. How about 'koopa install --all'.
** DONE Rework 'koopa install-default-apps' as 'koopa install'.
** TODO Consider reorganizing 'koopa uninstall-non-default-apps' to `koopa uninstall --all-non-default-apps`.
** TODO We need to rework and declutter some koopa CLI commands:
    - install, install-all-apps, install-default-apps
    - list-all-apps, list-default-apps
    - uninstall, uninstall-non-default-apps
    - koopa system update-tex-packages to koopa update system tex-packages
    - Consider moving `koopa list-all-apps` and `koopa-list-default-apps` to `koopa system list-all-apps`, `koopa system list-default-apps`
** TODO Move `koopa system create-dmg` to `koopa run create-dmg`?
** TODO Move `koopa system force-eject` to `koopa system admin force-eject`?
** TODO `koopa system --help` doesn't list supported subcommands:
    $ koopa system --help
    usage: koopa system [-h] ...

    positional arguments:
    remainder

    options:
    -h, --help  show this help message and exit
** TODO `koopa app --help` is also not informative.
** TODO Not clear what we mean in `koopa system` currently. We need to reorganize commands that require admin/sudo here:
    ~/.local/share/koopa develop a6678ee [!] 📦 0.16.0 <python 3.12.13: .venv>
    $ koopa system
    check                          force-eject                    reload-autofs
    clean-launch-services          hostname                       spotlight
    create-dmg                     ifactive                       switch-to-develop
    disable-passwordless-sudo      info                           test
    disable-touch-id-sudo          list                           update-tex-packages
    enable-passwordless-sudo       os-string                      version
    enable-touch-id-sudo           prefix                         which
    flush-dns                      prune-apps                     zsh-compaudit-set-permissions
    We're mixing things that help manage koopa install, and other things manage the system machine and require sudo permissions. That's not clear currently.
** TODO Reduce the number of dependencies when building r and r-devel from source. For example, we don't want to include gdal here.
** TODO Our language regarding "system" in the koopa CLI is inconsistent. Some commands require system admin privileges, some don't. This applies to `koopa update --all-system` for example.
** TODO Evaluate and consider reworking our flow with:
    - koopa install --system
    - koopa install --user
    - koopa uninstall --system
    - koopa uninstall --user
    Does it make more sense to use `koopa install system <APP>` and `koopa install user <APP>`? I think I like that better.
* Low complexity
** DONE Remove `koopa internal`
** DONE Remove 'koopa version' in favor of 'koopa --version'.
** DONE Can we add support for 'koopa -v' resolving to 'koopa --version'.
** DONE Consider reverting black and mypy build from source optimizations. Maybe not worth tradeoff of dealing with corporate environment URL issues.
** TODO Remove `koopa system test`
    $ koopa system test
    Error: tests prefix does not exist: '/Users/stmich14/.local/share/koopa/etc/koopa/tests'.
** TODO `koopa system spotlight` is very unclear.
