#!/usr/bin/env zsh
set -Eeu -o pipefail

# Install oh-my-zsh
# Modified 2019-06-20.

zsh_dir="${HOME}/.oh-my-zsh"
rm -rf "$zsh_dir"
git clone https://github.com/robbyrussell/oh-my-zsh "$zsh_dir"
unset -v zsh_dir
