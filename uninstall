#!/usr/bin/env bash
set -Eeu -o pipefail

# shellcheck source=/dev/null
source "$(koopa header bash)"

# Uninstall koopa.
# Updated 2019-08-14.

# Consider an improved approach to removing cellar links in `/usr/local/`.

if _koopa_has_sudo
then
    rm -fv /etc/profile.d/koopa.sh
fi

# shellcheck source=/dev/null
. "${KOOPA_HOME}/system/uninstall/dotfiles.sh"

# shellcheck source=/dev/null
. "${KOOPA_HOME}/system/uninstall/dotfiles-legacy.sh"

rm -frv "$(koopa config-dir)"
rm -frv "$KOOPA_HOME"
