#!/usr/bin/env bash

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" &>/dev/null && pwd -P)"
# shellcheck source=/dev/null
source "${script_dir}/../shell/bash/include/header.sh"

# """
# Default mode that doesn't load user config.
# Updated 2020-06-19.
# """

_koopa_assert_is_installed nvim

nvim -u NONE "$@"
