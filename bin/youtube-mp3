#!/usr/bin/env bash
set -Eeu -o pipefail

# shellcheck source=/dev/null
source "$(_koopa_header bash)"

# Download MP3 audio from YouTube or Soundcloud.
# Updated 2019-06-23.

_koopa_assert_is_installed youtube-dl

url="$1"

youtube-dl --extract-audio --audio-format mp3 "$url"
