#!/usr/bin/env bash
set -Eeu -o pipefail

# Extract MP3 audio from YouTube or SoundCloud.
# Modified 2019-06-18.

# shellcheck source=/dev/null
source "${KOOPA_DIR}/include/shell/bash/functions.sh"

assert_is_installed youtube-dl

url="$1"

youtube-dl --extract-audio --audio-format mp3 "$url"
