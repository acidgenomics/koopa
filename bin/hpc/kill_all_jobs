#!/usr/bin/env bash
set -Eeuo pipefail

# Kill all active jobs for current user.
# 2018-11-24

if [[ "$HPC_SCHEDULER" == "slurm" ]]; then
    scancel -u "$USER"
elif [[ "$HPC_SCHEDULER" == "lsf" ]]; then
    bkill 0
fi
