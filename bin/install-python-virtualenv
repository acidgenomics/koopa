#!/usr/bin/env bash
set -Eeuxo pipefail

# Set up python virtual environment.

# Check that Python and virtualenv are installed.
command -v python
command -v virtualenv

env_dir="${HOME}/virtualenvs"
env_name="python"

virtualenv "${env_dir}/${env_name}" --system-site-packages
