#!/usr/bin/env bash
set -Eeuxo pipefail

# Download a thumbnail image from YouTube.

url="$1"
id=$(echo "$url" | grep -Eo 'v=[A-Za-z0-9_]+' | cut -d '=' -f 2)

# Alternatively, can use hqdefault.jpg
wget -O "$id" "https://i.ytimg.com/vi/${id}/maxresdefault.jpg"
