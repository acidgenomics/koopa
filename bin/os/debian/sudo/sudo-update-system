#!/usr/bin/env bash
set -Eeu -o pipefail

# Update Debian.
# Modified 2019-06-18.

# shellcheck source=/dev/null
source "${KOOPA_DIR}/include/os/debian/sudo.sh"

sudo apt-get update
sudo apt-get -y dist-upgrade
