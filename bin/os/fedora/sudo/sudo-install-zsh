#!/usr/bin/env bash
set -Eeuo pipefail

# Z shell
#
# See also:
# - http://www.zsh.org/
# - http://zsh.sourceforge.net/Arc/source.html
# - https://github.com/robbyrussell/oh-my-zsh/wiki/Installing-ZSH

# shellcheck source=/dev/null
source "${KOOPA_DIR}/include/os/fedora/sudo.sh"

prefix="$KOOPA_PREFIX"
build_name="$KOOPA_OS_BUILD_STRING"
build_dir="${KOOPA_TMP}/build/zsh"

version="$(koopa_version zsh)"

echo "Installing zsh ${version}."

# Install build dependencies.
sudo yum install -y zsh
sudo -n yum-builddep -y zsh

# SC2103: Use a ( subshell ) to avoid having to cd back.
(
    rm -rf "$build_dir"
    mkdir -p "$build_dir"
    cd "$build_dir"
    wget https://sourceforge.net/projects/zsh/files/zsh/${version}/zsh-${version}.tar.xz/download
    tar -xJvf "download"
    cd "zsh-${version}"
    ./configure \
        --build="$build_name" \
        --prefix="$prefix"
    make
    make check
    # make test
    sudo -n make install
    rm -rf "$build_dir"
)

echo "zsh installed successfully."
command -v zsh
zsh --version

if grep -qv "${prefix}/bin/zsh" /etc/shells
then
    echo "Updating /etc/shells."
    sudo -n bash -c "echo ${prefix}/bin/zsh >> /etc/shells"
fi

echo "Updating default shell."
sudo -n chsh -s "${prefix}/bin/zsh" "$USER"

