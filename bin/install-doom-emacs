#!/usr/bin/env bash
set -Eeu -o pipefail

# Install Doom Emacs.
# Modified 2019-06-18.

if [[ -d ".emacs.d" ]]
then
    >&2 printf "Error: '.emacs.d/' directory exists.\n"
    exit 1
fi

git clone https://github.com/hlissner/doom-emacs ~/.emacs.d
~/.emacs.d/bin/doom quickstart
