#!/usr/bin/env bash
set -Eeuxo pipefail

# Upgrade koopa installation.

"${KOOPA_BASE_DIR}/install/update.sh"

cat << EOF
koopa updated successfully.
Shell must be reloaded for changes to take effect.
EOF
