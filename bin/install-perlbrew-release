#!/usr/bin/env bash
set -Eeuo pipefail

# Perlbrew
# https://perlbrew.pl/

source "${KOOPA_DIR}/includes/shell/bash/functions.sh"

version="$(koopa_version perl)"

# > perlbrew available
# > perlbrew list

perlbrew install "perl-${version}"
perlbrew switch "perl-${version}"
