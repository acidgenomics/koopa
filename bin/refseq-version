#!/usr/bin/env bash
set -Eeu -o pipefail

# shellcheck source=/dev/null
source "$(koopa header bash)"

# RefSeq release version.
# Updated 2019-09-20.

wget -q -O - ftp://ftp.ncbi.nlm.nih.gov/refseq/release/RELEASE_NUMBER
