#!/usr/bin/env bash
set -Eeu -o pipefail

# List PATH priority.
# Modified 2019-06-18.

# See also:
# - https://askubuntu.com/questions/600018

# Alternate approaches:
# > sed 's/:/\n/g' <<< "$PATH"
# > tr ':' '\n' <<< "$PATH"

# This uses bash parameter expansion.
echo "${PATH//:/$'\n'}"
