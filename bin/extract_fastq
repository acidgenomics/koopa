#!/usr/bin/env bash

# Extract FASTQs files inside a TAR file.
# 2018-09-22

file="$1"
if [[ ! -f "$file" ]]; then
    echo "${file} tar file does not exist"
    exit 1
fi
tar -xvf "$file" --wildcards "*.fastq.*"
