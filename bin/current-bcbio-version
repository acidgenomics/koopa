#!/usr/bin/env bash

# FIXME Move this into 'koopa current-bcbio-version'

# shellcheck source=/dev/null
. "$(dirname "${BASH_SOURCE[0]}")/../lang/shell/bash/include/header.sh"

koopa::current_bcbio_version "$@"
