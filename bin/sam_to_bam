#!/usr/bin/env bash
set -Eeuxo pipefail

# Convert SAM file to BAM file.
# 2018-09-22

command -v samtools >/dev/null 2>&1 || {
    echo >&2 "samtools missing"
    exit 1
}

for file in $(cat files); do
    samtools view -bS aligned/"${file}".Aligned.out.sam -o aligned/"${file}".bam;
done
