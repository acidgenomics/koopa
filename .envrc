#!/usr/bin/env bash

# Load the '.env' file, if present.
dotenv_if_exists

# Put the project virtualenv on PATH (relocation-proof; resolved vs $PWD).
if [[ -d '.venv/bin' ]]
then
    export VIRTUAL_ENV="$(expand_path .venv)"
    export VIRTUAL_ENV_PROMPT='.venv'
    PATH_add '.venv/bin'
fi
