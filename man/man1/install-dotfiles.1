



    [url="${url}"]

Install dot files repository.

required positional arguments with defaults:
    1.  Dot file repository URL.
        Defaults to '${url}'.

$(_koopa_help_args)

details:
    Installs into '${target_dir}'.
    Adds a symlink in koopa config dir.

note:
    Bash script.
    Updated 2019-10-09.
