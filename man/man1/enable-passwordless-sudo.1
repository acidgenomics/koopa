


    [user="${user}"]

Enable passwordless sudo access.

required positional arguments with defaults:
    1.  User name.
        Defaults to '${user}'.

$(_koopa_help_args)

details:
    User configuration file is saved in '/etc/sudoers.d/'.

note:
    Bash script.
    Updated 2019-10-01.
