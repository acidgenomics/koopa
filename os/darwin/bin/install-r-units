#!/usr/bin/env Rscript

# FIXME Need to add documentation.
# FIXME Switch to bash script?

install.packages(
    pkgs = "units",
    type = "source",
    configure.args = c(
        "--with-udunits2-lib=/usr/local/lib",
        "--with-udunits2-include=/usr/include/udunits2"
    )
)
