#!/usr/bin/env bash
set -Eeu -o pipefail

# shellcheck source=/dev/null
source "$(_koopa_header darwin)"

# Convert UTF-8 NFD to NFC.
# Updated 2019-06-18.

_koopa_is_installed convmv

# Use `-i` flag for interactive.

convmv -r -f utf8 -t utf8 --nfc --notest .
