#!/usr/bin/env bash
set -Eeu -o pipefail

# shellcheck source=/dev/null
source "$(koopa header darwin)"

# Install OpenSSL.
# Updated 2019-06-23.

# shellcheck source=/dev/null
source "$(_koopa_cellar_script openssl)"
