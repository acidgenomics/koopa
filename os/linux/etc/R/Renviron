# R environment for Linux.
# Updated 2019-07-10.

# Restrict `$PATH` so we don't load any binaries from conda or bcbio.
PATH="/usr/local/bin:/usr/bin:/bin"

# User library
# I'm currently using a symlink approach here to manage my libraries.
R_LIBS_USER="~/R/%p-library/%v"

# Always upgrade GitHub R packages, without prompting.
# See `remotes::update_packages()` for details.
R_REMOTES_UPGRADE="always"

# R can sometimes error due to time zone, unless this is set.
TZ="America/New_York"
