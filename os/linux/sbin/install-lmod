#!/usr/bin/env bash

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd -P)"
# shellcheck source=/dev/null
source "${script_dir}/../include/header.sh"
_acid_assert_is_installed lua luarocks

name="lmod"
version="$(_acid_variable "$name")"

prefix="$(_acid_app_prefix)/${name}"
_acid_assert_is_not_dir "$prefix"

apps_dir="${prefix}/apps"
data_dir="${prefix}/moduleData"

tmp_dir="$(_acid_tmp_dir)/${name}"

_acid_message "Installing ${name} ${version}."

(
    rm -fr "$tmp_dir"
    mkdir -pv "$tmp_dir"
    cd "$tmp_dir" || exit 1
    file="${version}.tar.gz"
    url="https://github.com/TACC/Lmod/archive/${file}"
    _acid_download "$url"
    _acid_extract "$file"
    cd "Lmod-${version}" || exit 1
    ./configure \
        --prefix="$apps_dir" \
        --with-spiderCacheDir="${data_dir}/cacheDir" \
        --with-updateSystemFn="${data_dir}/system.txt"
    sudo make install
    rm -fr "$tmp_dir"
)

# bash, zsh:
sudo ln -fnsv "${apps_dir}/lmod/lmod/init/profile" \
    "/etc/profile.d/z00_lmod.sh"
# csh, tcsh:
sudo ln -fnsv "${apps_dir}/lmod/lmod/init/cshrc" \
    "/etc/profile.d/z00_lmod.csh"
# fish:
sudo ln -fnsv "${apps_dir}/lmod/lmod/init/profile.fish" \
    "/etc/fish/conf.d/z00_lmod.fish"

# > module --version
# > module spider
