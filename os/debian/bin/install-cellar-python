#!/usr/bin/env bash

# shellcheck source=/dev/null
source "$(koopa header debian)"

# Install Python.
# Updated 2019-06-24.

if _koopa_has_sudo
then
    sudo apt-get -y install python3 python3-pip
fi

source "$(koopa cellar-script python)"
