#!/usr/bin/env bash
set -Eeu -o pipefail

# shellcheck source=/dev/null
source "$(koopa header debian)"

if _koopa_has_sudo
then
    sudo apt-get -y build-dep coreutils
fi

# shellcheck source=/dev/null
source "$(_koopa_cellar_script coreutils)"
