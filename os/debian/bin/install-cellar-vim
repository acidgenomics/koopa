#!/usr/bin/env bash

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd -P)"
# shellcheck source=/dev/null
source "${script_dir}/../include/header.sh"

if _acid_is_shared
then
    sudo apt-get -y install vim
    sudo apt-get -y build-dep vim
fi

_acid_cellar_script vim
