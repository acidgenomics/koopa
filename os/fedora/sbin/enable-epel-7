#!/usr/bin/env bash
set -Eeu -o pipefail

# shellcheck source=/dev/null
source "$(koopa header fedora)"
_koopa_assert_has_sudo



# Usage                                                                     {{{1
# ==============================================================================

usage() {
cat << EOF
$(_koopa_help_header)

Enable EPEL repository.

$(_koopa_help_args)

see also:
    https://aws.amazon.com/premiumsupport/knowledge-center/ec2-enable-epel/

note:
    Bash script.
    Updated 2019-09-26.
EOF
}

_koopa_help "$@"



# Script                                                                    {{{1
# ==============================================================================

os_type="$(_koopa_os_type)"

# Only allow install on RHEL 7.
if [[ "$os_type" != "rhel7" ]]
then
    printf "RHEL 7 is required.\n"
    exit 1
fi

# Check if EPEL is already enabled.
if sudo yum repolist | grep -q "epel/"
then
    printf "EPEL 7 is already enabled.\n"
    exit 0
fi

sudo yum install -y \
    https://dl.fedoraproject.org/pub/epel/epel-release-latest-7.noarch.rpm

sudo yum-config-manager --enable epel

# Here's how to disable, for reference.
# > sudo yum-config-manager --disable epel

sudo yum -y install epel-release
