#!/usr/bin/env bash

# shellcheck source=/dev/null
source "$(koopa header fedora)"

# Install GNU core utilities.
# Modified 2019-06-23.

if _koopa_has_sudo
then
    sudo yum-builddep -y coreutils
fi

source "$(koopa cellar-script coreutils)"
