#!/usr/bin/env bash

# shellcheck source=/dev/null
source "$(koopa header fedora)"

# Install Z shell.
# Modified 2019-06-23.

if _koopa_has_sudo
then
    sudo yum install -y zsh
    sudo yum-builddep -y zsh
fi

source "$(koopa cellar-script zsh)"
