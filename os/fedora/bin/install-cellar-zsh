#!/usr/bin/env bash
set -Eeu -o pipefail

# shellcheck source=/dev/null
source "$(koopa header fedora)"

# Install Z shell.
# Updated 2019-07-09.

os_type="$(_koopa_os_type)"

if _koopa_has_sudo
then
    sudo yum install -y zsh
    if [[ "$os_type" == "rhel7" ]]
    then
        sudo yum-builddep -y zsh
    fi
fi

# shellcheck source=/dev/null
source "$(_koopa_cellar_script zsh)"
