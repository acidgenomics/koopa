#!/usr/bin/env bash
set -Eeu -o pipefail

# shellcheck source=/dev/null
source "$(koopa header fedora)"

# Install Bash.
# Updated 2019-07-09.

os_type="$(koopa os-type)"

if _koopa_has_sudo
then
    sudo yum install -y bash
    if [[ "$os_type" == "rhel7" ]]
    then
        sudo yum-builddep -y bash
    fi
fi

# shellcheck source=/dev/null
source "$(_koopa_cellar_script bash)"
