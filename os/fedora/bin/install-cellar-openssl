#!/usr/bin/env bash

# shellcheck source=/dev/null
source "$(koopa header fedora)"

# Install OpenSSL.
# Modified 2019-06-23.

if _koopa_has_sudo
then
    sudo yum install -y openssl
    sudo yum-builddep -y openssl
fi

source "$(koopa cellar-script openssl)"
