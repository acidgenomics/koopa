#!/usr/bin/env bash
set -Eeu -o pipefail

# Install base programs recommended for Amazon Linux 2.
# Modified 2019-06-20.

# shellcheck source=/dev/null
source "${KOOPA_DIR}/system/include/os/amzn/sudo.sh"

install-fedora-base

# Install utils such as chsh.
sudo yum install -y util-linux-user
